layer: 'scaffolding'

project:
  title: 'ghd'
  description: 'Workspace-level configuration, documentation, and automation for GitHub Downloader.'
  owner: 'meigma'
  maintainers:
    - 'meigma'

workspace:
  inheritedTasks:
    include: []

tasks:
  lint:
    command: 'go run github.com/golangci/golangci-lint/v2/cmd/golangci-lint@v2.11.4 run ./... --show-stats=false'
    toolchains: ['go']
    inputs:
      - '.golangci.yml'
      - 'cmd/**/*.go'
      - 'example/**/*.go'
      - 'go.mod'
      - 'go.sum'
      - 'internal/**/*.go'
    options:
      cache: false

  test:
    command: 'go test ./...'
    toolchains: ['go']
    inputs:
      - 'cmd/**/*.go'
      - 'example/**/*.go'
      - 'go.mod'
      - 'go.sum'
      - 'internal/**/*.go'
    options:
      cache: false

#   format:
#     command: 'prettier --check .'
#     toolchains: ['javascript', 'node', 'npm']
#     inputs: []
#     options:
#       cache: false
#
#   docs-build:
#     command: 'moon run docs:build'
#     toolchains: 'system'
#     inputs: []
#     options:
#       cache: false
